root = "./"
project_root = "../../"

thread = 2
logger = nil
harbor = 0
start = "luaprof_smoke"
bootstrap = "snlua bootstrap"

cpath = root .. "cservice/?.so"
luaservice = project_root .. "examples/skynet/?.lua;" .. root .. "service/?.lua"
lualoader = root .. "lualib/loader.lua"
lua_path = root .. "lualib/?.lua;" .. root .. "lualib/?/init.lua"
lua_cpath = "$LUAPROF_MODULE_CPATH;" .. root .. "luaclib/?.so"
